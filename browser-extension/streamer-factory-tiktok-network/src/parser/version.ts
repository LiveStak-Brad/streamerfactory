/** Bump when parser behavior changes in a way that affects stored data. */
export const PARSER_VERSION = "1a.5";

/** Extension package version stamped on sync payloads. */
export const EXTENSION_VERSION = "0.2.7";
